\usepackage{fontspec}
\setmainfont{Times New Roman}
\defaultfontfeatures{Ligatures=TeX}

\usepackage{polyglossia}
\setmainlanguage{vietnamese}

\usepackage{geometry}
\geometry{a4paper, left=3cm, right=2cm, top=2.5cm, bottom=2.5cm}

\usepackage{fancyvrb}
\usepackage{color}

\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{float}

\usepackage{titlesec}
\titleformat{\chapter}[hang]
{\normalfont\huge\bfseries} % kiểu chữ
{\thechapter.}              % đánh số chương: "1."
{1em}                       % khoảng cách giữa số và tiêu đề
{}                          % phần xử lý nội dung phía sau số

\renewcommand{\normalsize}{\large}

\usepackage[
colorlinks=true,
linkcolor=black,     % mục lục giữ màu đen
urlcolor=blue,       % link ngoài có màu xanh
citecolor=black,     % citation giữ màu đen
pdfborder={0 0 1}    % gạch chân link ngoài
]{hyperref}

\usepackage{listings}
\renewcommand{\lstlistingname}{Ví dụ}

\lstset{
	basicstyle=\ttfamily\footnotesize,
	breaklines=true,
	frame=single,
	backgroundcolor=\color{gray!10},
	keywordstyle=\color{blue},
	commentstyle=\color{green!50!black},
	stringstyle=\color{red},
	showstringspaces=false,
	language=Python,
	numbers=left,
	numberstyle=\tiny\color{gray}
}

% Định nghĩa ngôn ngữ YAML cho listings
\lstdefinelanguage{yaml}{
	keywords={true,false,null,y,n},
	keywordstyle=\color{blue}\bfseries,
	basicstyle=\ttfamily\small,
	sensitive=false,
	comment=[l]{\#},
	morecomment=[l]{\#},
	morestring=[b]",
	morestring=[b]'
}

\usepackage[normalem]{ulem}
\usepackage{amssymb}          
\usepackage{pifont}


